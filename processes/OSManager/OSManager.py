
# 02) Create a class called OSManager, that receives a directory as parameter and works on the files inside the directory
# 03) Inside the OSManager class, create a method that reads all the files inside the directory
# 04) Inside the OSManager class, create a method that get the most recent element
# 05) Inside the OSManager class, create a method that renames a specific file based on the parameter (Account)
class OSManager:
    
    def read_files(directory):
        pass

    def get_most_recent_element(directory):
        pass

    def rename(directory, account_number):
        pass

